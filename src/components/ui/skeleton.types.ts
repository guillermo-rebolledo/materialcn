/** The type roles a skeleton can size itself to. Mirrors the M3 type scale. */
export type SkeletonTextRole =
  | `display-${"lg" | "md" | "sm"}`
  | `headline-${"lg" | "md" | "sm"}`
  | `title-${"lg" | "md" | "sm"}`
  | `body-${"lg" | "md" | "sm"}`
  | `label-${"lg" | "md" | "sm"}`
