# 14 — Add docked single-date DatePicker

**What to build:** Add a localized single-date picker that combines a Material calendar with a field and docked popover presentation.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Consumers can select, clear, and control a single date from a field and docked calendar.
- [ ] Month and year navigation, today, selected, outside-month, disabled, and unavailable dates are represented.
- [ ] Keyboard navigation and focus movement follow accessible calendar conventions.
- [ ] Display and parsing behavior can be localized without hard-coded date formats.
- [ ] Field validation and disabled/read-only states integrate with the existing Field API.
- [ ] Stories cover core states, keyboard use, locale variation, and light/dark presentation.
- [ ] Typecheck, lint, browser tests, and the library build pass.
